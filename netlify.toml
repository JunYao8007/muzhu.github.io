[build]
  command = "hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.152.2"       # 精准匹配你本地的 0.152.2 版本
  HUGO_EXTENDED = "true"          # 启用扩展版（你的版本带 extended）
  HUGO_ENV = "production"         # 生产环境标识，避免路径异常